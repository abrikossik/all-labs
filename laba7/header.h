#ifndef HEADER
#define HEADER

#include <iostream>
#include <string>
#include <algorithm>

bool IsDigitOnly(const std::string&);
bool IsAlphaOnly(const std::string&);
bool IsAlnumMixed(const std::string&);
std::string CheckWord(std::string&);
std::string CheckString(std::string&);

#endif HEADER